{
  "name": "@stencil-pm/claude-code-plugin",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin adapter for Stencil template management",
  "dependencies": {
    "@stencil-pm/core": "workspace:*"
  },
  "scripts": {
    "test": "node --test",
    "lint": "bash -n scripts/*.sh && bash -n scripts/lib/*.sh"
  }
}
