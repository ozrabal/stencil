#!/usr/bin/env bash
# resolve-template.sh — stub: calls core CLI to resolve a template
# Usage: resolve-template.sh <template-name> [--output <path>]

set -euo pipefail

# TODO: invoke the @stencil-pm/core CLI with the given template name
echo "resolve-template: not yet implemented" >&2
exit 1
