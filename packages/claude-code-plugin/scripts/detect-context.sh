#!/usr/bin/env bash
# detect-context.sh — stub: detects environment context variables for placeholder resolution
# Outputs key=value pairs to stdout

set -euo pipefail

# TODO: detect git metadata, env vars, and other context sources
echo "detect-context: not yet implemented" >&2
exit 1
